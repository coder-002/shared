import React from "react";
import NepaliDatePicker, { TNepaliDatePickerProps } from "./NepaliDatePicker";

const DatePickerNp = (props: TNepaliDatePickerProps) => (
  <NepaliDatePicker {...props} />
);
export { DatePickerNp };
